{
  // Claude が使って良いツールのホワイトリスト
  "allowedTools": [
    "bash",          // npm / bun / git / gh… ぜんぶ OK
    "playwright"     // mcp__playwright__browser_* 系 API
  ],

  // MCP サーバーの起動方法
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--headless"]
    }
  }
}